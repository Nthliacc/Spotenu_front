import styled from 'styled-components';
import back from '../../imagens/unsplash.jpg';

export const HomeContainer = styled.main`
    max-width: 100vw;
    height: 100vh;
    background-image: url(${back});
    color: white;
`;